DebugWindow.cs implements P1 debug panel with three sections: Ping, ResolveAll, Config Preview + Reload.
The entry gizmo is injected into Pawn.GetGizmos() only when Prefs.DevMode is true and DebugPanelEnabled is true.


